I've changed some from default

command = control
`ctrl` + `n` = new tab
`crtl` + `t` = insert template (not from the templater plug-in)
`ctrl` + `p` = shows all commands
`ctrl` + `o` = open any note from anywhere, can also create a new note

